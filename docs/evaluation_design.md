# Evaluation Design

The evaluation layer scores outputs during the workflow instead of treating evaluation as a separate notebook step.

Supported first-version metrics:

- OCR character error rate
- ASR word error rate
- ASR character error rate
- Field-level exact match
- Field-level accuracy
- Summary coverage mock score
- Retrieval hit rate

